(function(root,factory){const api=factory();if(typeof module==="object"&&module.exports)module.exports=api;else root.LKAssetSearch=api;})(typeof globalThis!=="undefined"?globalThis:this,function(){"use strict";function normalize(value){return String(value==null?"":value).normalize("NFKC").toLocaleLowerCase("zh-Hant").trim();}function tokens(query){return normalize(query).split(/\s+/u).filter(Boolean);}function indexText(asset){return normalize([asset.id,asset.emoji,asset.name,asset.path,asset.category,asset.status,asset.description,...(Array.isArray(asset.tags)?asset.tags:[])].join(" "));}function matchesQuery(asset,query){const wanted=tokens(query);if(!wanted.length)return true;const haystack=indexText(asset);return wanted.every(token=>haystack.includes(token));}function filterAssets(assets,options){const safe=Array.isArray(assets)?assets:[];const opts=options||{};return safe.filter(asset=>{if(opts.category&&asset.category!==opts.category)return false;if(opts.status&&asset.status!==opts.status)return false;return matchesQuery(asset,opts.query||"");});}function resolveAssetHref(prefix,path){return String(prefix||"")+String(path||"").replace(/^\/+/,"");}return Object.freeze({normalize,tokens,indexText,matchesQuery,filterAssets,resolveAssetHref});});
