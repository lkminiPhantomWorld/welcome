name: Gatekeeper
on: [push, pull_request]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 驗證 LKMini 公開種子
        run: python3 tools/verify_lkmini.py

      - name: 暴力驗證公開傳送門
        run: python3 tools/verify_portal.py
