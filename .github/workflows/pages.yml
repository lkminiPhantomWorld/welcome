name: Historical duplicate Pages workflow disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-retirement:
    runs-on: ubuntu-latest
    steps:
      - name: Preserve historical workflow as inactive evidence
        run: |
          echo "完成: historical duplicate workflow retained as evidence"
          echo "active=false"
          echo "authorized=false"
          echo "current_deployer=.github/workflows/deploy-pages.yml"
          echo "current_projection=https://lkmini-wiring-hub.ky46738.chatgpt.site"
